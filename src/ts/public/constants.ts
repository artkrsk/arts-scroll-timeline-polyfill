/** Internal runtime data; the package root exports only the derived type. */
export const RANGE_NAMES = [
  'entry',
  'exit',
  'cover',
  'contain',
  'entry-crossing',
  'exit-crossing',
] as const
